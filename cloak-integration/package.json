{
  "name": "@legacy-protocol/cloak-integration",
  "version": "1.0.0",
  "description": "Cloak SDK integration layer for Legacy Protocol — shielded inheritance vault execution",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@cloak.dev/sdk": "latest",
    "@legacy-protocol/sdk": "^0.3.1",
    "@solana/web3.js": "^1.95.3",
    "@solana/spl-token": "^0.4.8"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "@types/node": "^20.0.0"
  }
}
